const settings = require("../../config/settings");

module.exports.run = function (reaction, user, bot) {
    return new Promise(function (resolve, reject) {
      

    });
}

module.exports.properties = {
    name: "",
    description: "",
    usage: "react with :blank: to",
    blacklist: [],
    useWhitelist: true
}